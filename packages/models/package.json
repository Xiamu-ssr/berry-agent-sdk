{
  "name": "@berry-agent/models",
  "version": "0.5.0-alpha.1",
  "description": "Provider presets, model aggregation, failover resolvers, and tier shortcuts for Berry Agent SDK.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc",
    "test": "vitest run --maxWorkers=2"
  },
  "peerDependencies": {
    "@berry-agent/core": "^0.5.0-alpha.1"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@berry-agent/core": "*",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "license": "MIT"
}
