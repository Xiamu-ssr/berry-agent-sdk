{
  "name": "@berry-agent/team",
  "version": "0.5.0-alpha.1",
  "description": "Team mode for Berry Agent SDK — leader/teammate topology, inter-agent messaging, persistent project-scoped collaboration",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc",
    "test": "vitest run --maxWorkers=2"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "peerDependencies": {
    "@berry-agent/core": "^0.5.0-alpha.1"
  },
  "devDependencies": {
    "@berry-agent/core": "*",
    "vitest": "^3.2.4",
    "typescript": "^5.8.3"
  },
  "license": "MIT"
}
